import { LightBoxDirective } from './light-box.directive';

describe('LightBoxDirective', () => {
  it('should create an instance', () => {
    const directive = LightBoxDirective;
    expect(directive).toBeTruthy();
  });
});
